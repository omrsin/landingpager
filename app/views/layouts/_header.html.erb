<header class="fixed-menu">
	<h1 class="inset-shadow-text">Landingpager</h1>
	<div class="header-form">
		<%= form_tag({ controller: "core", action: "generate", method: "post" }, onsubmit: "updateSubmitInfo()", ) do %>
			<%= hidden_field_tag "html_string", "This is just a test"  %>
			<%= submit_tag("Generar", class: "btn inset-shadow-text") %>
		<% end %>	
	</div>
</header>
